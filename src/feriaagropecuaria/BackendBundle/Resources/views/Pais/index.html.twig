{% extends "::adminlayaout.html.twig" %}

{% block titulo -%}Pais list{% endblock %}
{% block body -%}

<div class="table-responsive">
    <table class="records_list table table-striped">
        <thead>
            <tr>
                <th>Idpais</th>
                <th>Nombre</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('pais_show', { 'id': entity.idPais }) }}">{{ entity.idPais }}</a></td>
                <td>{{ entity.Nombre }}</td>
                <td>

                        <a href="{{ path('pais_show', { 'id': entity.idPais }) }}"><button type="button" class="btn btn-default">show</button></a>

                        <a href="{{ path('pais_edit', { 'id': entity.idPais }) }}"><button type="button" class="btn btn-default">edit</button></a>

                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>

            <a href="{{ path('pais_new') }}">
                <button type="button" class="btn btn-primary">Create a new entry</button>
            </a>

    {% endblock %}
